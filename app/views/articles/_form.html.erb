<%= form_for(@article) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <br>
  <%#= f.button f.object.new_record? ? 'Create' : 'Update' %>
  <%= f.submit %>
<% end %>
