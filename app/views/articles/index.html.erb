

<% @article.each do |article| %>
  <div class="articles">
    <h2><%= link_to "#{article.title}", article_path(article), class: "article" %></h2>
    <p><%= article.content %></p>
  </div>
<% end %>

